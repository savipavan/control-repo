---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      #- "data/team/%{team}/fqdn/%{fqdn}.yaml"
      #- "data/team/%{team}/customization/%{customization}.yaml"
      #- "data/team/%{team}/role/%{role}.yaml"
      - "data/team/%{team}/profile/%{profile}.yaml"
      #- "data/team/%{team}/site/%{site}.yaml"
      #- "common.yaml"
